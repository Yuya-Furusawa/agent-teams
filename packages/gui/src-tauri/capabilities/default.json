{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window (invoke + event listen)",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
